# 삭제 코너의 조 순회표 노출 방지 (compact)

## 요구사항
`groups.itinerary`(JSONB)는 코너 삭제 시 참조 무결성이 적용되지 않아, 삭제된 코너가 조 목록/상세
응답에 그대로 남아 노출되던 문제 수정.

## 왜 이렇게 했는가
`GroupService`가 현재 캠프의 코너 ID 집합을 조회해, 저장소 객체를 변경하지 않는 새 도메인
스냅샷으로 재구성하며 유효 ID만 남김 — 영속 데이터는 읽기 중 수정하지 않고, 조회 시점마다
정규화된 뷰만 계산.

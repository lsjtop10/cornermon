# 코너·트랙 관리 화면 계층 구조 전환 + 코너 삭제 UI

## 배경

관리자 대시보드에서 같은 코너가 2개씩 보이는 버그를 조사한 결과, 코너 생성 API가
중복 제출되어 트랙이 하나도 없는 "좀비 코너"가 `corners` 테이블에 실제로 쌓이는 것이
원인이었다. 좀비 코너는 트랙 기준으로 목록을 구성하는 `/corner-track-manage`
화면에는 나타나지 않아 관리자가 존재 자체를 인지하지 못했고, 삭제할 UI 진입점도
없었다(`deleteCornerProvider`는 정의만 있고 어떤 화면도 참조하지 않음).

백엔드는 별도 Issue로 다음을 요청한다 (이 Plan의 스코프 아님):
- `DELETE /corners/{id}`를 hard delete → soft delete로 변경 (요청/응답 계약은 동일하게 유지)
- 목록 조회(`ListCornerViewsByCamp`)에서 soft-delete된 코너 제외
- 트랙 이력이 전혀 없는 soft-delete 코너를 일정 기간 후 물리 삭제하는 배치/스크립트

프론트엔드는 위 계약(요청/응답 모양 불변)을 가정하고, 기존 `DELETE /corners/{id}`
API를 그대로 호출하는 UI만 구현한다. 백엔드가 hard→soft로 구현을 바꿔도 프론트
코드 변경은 필요 없다.

## 유즈케이스

| 우선순위 | 유즈케이스 | 설명 | 용도 |
| --- | --- | --- | --- |
| **P0** | UC-1: 코너→트랙 계층 표시 | `/corner-track-manage` 화면을 트랙 플랫 리스트에서 코너별 그룹(코너 헤더 + 하위 트랙) 구조로 전환 | **프로덕션 핵심 UI** |
| **P0** | UC-2: 트랙 0개 코너 삭제 | 코너 그룹 헤더에 트랙이 0개인 코너에 한해 "삭제" 버튼 노출, 확인 다이얼로그 후 `deleteCornerProvider` 호출 | **프로덕션 핵심 로직** (좀비 코너 정리) |
| P1 | UC-3: 삭제 실패 피드백 | 삭제 실패 시 커넥션 유실/API 에러 구분해 배너·SnackBar로 표시 (개발자가이드 §4 규칙 준수) | 안정성 |

**비목표**: 트랙이 1개 이상 있는 코너의 삭제(soft-delete 정책 확정 전까지 보류), 코너 생성 중복 제출 자체를 막는 것(별도 Issue의 idempotency key 대응 영역).

## 기존 코드 이해

- `frontend/lib/admin/features/track_bulk_manage/track_bulk_manage_screen.dart`: 현재 `trackListProvider`/`cornerListProvider`를 각각 watch해서 트랙을 플랫 `DataTable`로 그림. 코너 이름은 `cornerNames` 맵으로만 참조(179-201행 정렬 로직 등).
- `frontend/lib/shared/api/providers/corner_track_providers.dart:90-94`: `deleteCornerProvider`(`Future<void> deleteCorner(Ref ref, CornerId id)`) 존재하나 `retry: noRetry`가 없음 — 개발자가이드 §2.3 규칙(부작용 있는 액션 provider는 `retry: noRetry` 명시)을 위반하고 있어 이번 작업 범위에서 함께 수정한다.
- 기존 트랙 삭제(`_delete`, 99-131행) 패턴: BUSY 트랙 포함 시 차단 → 확인 다이얼로그 → provider 호출 → `setState`/`ref.invalidate`. 코너 삭제도 동일 패턴을 따른다.
- `AppButton`은 `size`/`width` 필수 파라미터 — 관리자 화면이므로 `AppButtonSize.compact` 사용(개발자가이드 §5).

## 객체 설계

```dart
/// 코너 1개 + 그 코너에 속한 트랙 목록. 화면 그룹핑 전용, provider 레이어에는 두지 않음.
class CornerTrackGroup {
  const CornerTrackGroup({required this.corner, required this.tracks});
  final api.Corner corner;
  final List<api.Track> tracks;

  bool get canDelete => tracks.isEmpty;
}

/// 코너 목록 + 트랙 목록을 코너 기준으로 묶는다. 트랙이 없는 코너도 그룹으로 포함되어야
/// 좀비 코너가 화면에 드러난다 (현재 버그의 핵심 — 반드시 트랙 유무와 무관하게 전체 코너를 순회).
List<CornerTrackGroup> groupTracksByCorner(
  List<api.Corner> corners,
  List<api.Track> tracks,
);
```

`_TrackBulkManageScreenState`에 추가할 메서드:

```dart
Future<void> _deleteCorner(CampId campId, api.Corner corner) async;
```

## 구현 단계

### Phase A: provider 정합성 수정 (예상 소요: 10분)

| 순서 | 작업 | 파일 |
| --- | --- | --- |
| A-1 | `deleteCorner` provider에 `@Riverpod(retry: noRetry)` 적용 (기존 파일 확장) | `frontend/lib/shared/api/providers/corner_track_providers.dart:90` |
| A-2 | `dart run build_runner build` 후 `lib/shared/api/gen` 삭제 여부 확인/복구 (개발자가이드 필수 절차) | - |

### Phase B: 코너→트랙 그룹핑 로직 (예상 소요: 30분)

| 순서 | 작업 | 파일 |
| --- | --- | --- |
| B-1 | `CornerTrackGroup`, `groupTracksByCorner` 추가 (신규, 순수 함수라 유닛 테스트 용이) | `track_bulk_manage_screen.dart` 상단 또는 `track_bulk_manage_grouping.dart` (신규 분리 여부는 파일 길이 보고 판단) |
| B-2 | 유닛 테스트: 트랙 0개 코너도 그룹에 포함되는지, 여러 코너에 걸친 트랙이 올바르게 분배되는지 | `frontend/test/admin/features/track_bulk_manage/` (신규) |

### Phase C: 화면 UI 전환 (예상 소요: 1.5시간)

| 순서 | 작업 | 파일 |
| --- | --- | --- |
| C-1 | `DataTable` 단일 테이블 → 코너별 `ExpansionTile`(또는 동급 그룹 위젯) + 그룹 내부에 기존 트랙 행 재사용 (기존 파일 대폭 수정) | `track_bulk_manage_screen.dart` |
| C-2 | 코너 그룹 헤더에 "삭제" 버튼 추가, `canDelete == false`면 비활성 + `disabledReason: '연결된 트랙이 있어 삭제할 수 없습니다'` (기존 `AppButton.disabledReason` 패턴 재사용, 246-256행 참고) | 동일 |
| C-3 | 기존 트랙 다중 선택(`_selectedIds`)·정렬·필터·CSV 내보내기 기능이 그룹 UI 전환 후에도 동일하게 동작하는지 확인 | 동일 |

### Phase D: 삭제 액션 구현 (예상 소요: 40분)

| 순서 | 작업 | 파일 |
| --- | --- | --- |
| D-1 | `_deleteCorner`: 확인 다이얼로그(`'코너 "{name}"을(를) 삭제하시겠습니까?'`) → `ref.read(deleteCornerProvider(CornerId(corner.id!)).future)` → 성공 시 `ref.invalidate(cornerListProvider(campId))` + SnackBar | `track_bulk_manage_screen.dart` |
| D-2 | 개발자가이드 §2.2 임시 리스너 패턴 적용 (액션 provider를 `ref.read(...future)`로 호출하므로 필수) | 동일 |
| D-3 | 개발자가이드 §4 규칙대로 `DioException` 커넥션 유실/API 에러 분기 처리 | 동일 |

## 검증 체크리스트

### 유즈케이스 검증
- [ ] UC-1: 트랙이 0개인 코너도 그룹 헤더로 화면에 노출된다 (현재 버그의 재발 확인 수단)
- [ ] UC-1: 기존 필터/정렬/다중 선택/CSV 내보내기가 그룹 UI에서도 정상 동작
- [ ] UC-2: 트랙 0개 코너에서만 삭제 버튼이 활성화된다
- [ ] UC-2: 삭제 확인 후 코너가 목록에서 사라지고 재조회(invalidate)된다
- [ ] UC-3: 네트워크 끊김 시 상단 배너, API 4xx/5xx 시 SnackBar로 구분 표시

### 코드 품질 검증
- [ ] `deleteCornerProvider`에 `retry: noRetry` 적용됨
- [ ] `dart run build_runner build` 이후 `lib/shared/api/gen` 손상 없음 확인
- [ ] `flutter test test/admin/features/track_bulk_manage/` 통과
- [ ] `dart analyze lib/` 경고 없음
- [ ] 실기기(관리자 앱, `make run-admin`)에서 좀비 코너 생성(코너 추가 더블클릭) → 그룹 목록에 노출 → 삭제 → 사라짐 확인

## 커밋 분할 (300 loc 기준)
1. Phase A + B (provider 수정 + 그룹핑 로직 + 유닛 테스트)
2. Phase C (화면 UI 전환)
3. Phase D (삭제 액션)

# 관리자 다이렉트 읽음 상태 정합성 수정 (compact)

## 요구사항
대시보드·좌측 트랙 목록의 메시지 미리보기 조회가 읽음 처리를 함께 트리거해서 unread 배지가
의도치 않게 사라지는 문제 수정. 실제 스레드를 연 경우에만 읽음 처리되어야 함.

## 왜 이렇게 했는가
provider 호출에 `background` 플래그를 구분 — 미리보기는 `background: false`(읽음 처리 안 함),
스레드 열람·전송 후 재조회만 `background: true`(읽음 처리)로 호출 경계를 명확히 함.
